<%= form_with(model: @tweet, local: true) do |f| %>
<div class= "form-group"> <%= f.label :content %> <%= f.text_field :content, class: 'form-control' %></div>

<div class= "form-group"> <%= f.submit :Twittear %> </div>
<% end %>