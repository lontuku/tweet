

<% if current_user != nil %>
<div class= container>
<div class="btn-group btn-group-toggle" data-toggle="buttons">
<%= link_to 'TODOS', todos_path, class: "btn btn-info" %>
<%= link_to 'SEGUIDOS', root_path, class: "btn btn-dark" %>
</div>
</div>
    <%= render 'form' %>
    <%= render 'usercontacts' %>
<% else %>
    <%= render 'form' %>
    <%= render 'tweets' %>
<div class="container">
    <%= paginate @tweets %>
</div>
<% end %>
